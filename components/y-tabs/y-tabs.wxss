.tabs_title {
  display: flex;
}
.title_item {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 16rpx 0;
}
.active {
  color: var(--themeColor);
  border-bottom: 4rpx solid currentColor;
}